<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vertical Scrolling Page with Sidebar and Top Bar</title>
    <!-- Font Awesome for icons (bell and user) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Add Inter from Google Fonts (customize weights/styles as needed) -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <!-- UIcons Core (required base for all styles) - Uncommented to enable icons -->
    <link rel="stylesheet" href="https://cdn-uicons.flaticon.com/2.6.0/uicons-regular-rounded/css/uicons-regular-rounded.css">
    <style>
        /* Enable smooth scrolling for anchor links (though not used now) */
        html {
            scroll-behavior: smooth;
        }
        
        /* Reset and base styles */
        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
            overflow-y: auto;
            font-family: Arial, sans-serif;
        }
        
        /* Top bar styles (now starts after sidebar, no overlap) */
        header {
            position: fixed;
            top: 0;
            left: 15%; /* Start after sidebar width */
            width: 85%; /* Span the remaining width */
            height: 60px;
            background-color: #f8f8f8; /* Light gray background */
            display: flex;
            align-items: center;
            justify-content: flex-end;
            padding: 0 20px;
            box-sizing: border-box;
            z-index: 1000;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        /* Search field */
        #search-field {
            padding: 10px 15px;
            width: 250px;
            border: 0px solid #ccc;
            border-radius: 5px;
            background-color: #f0f2fe;
            font-size: 16px;
            margin-left: 200px;
            /*margin-right: 200px;*/
        }
        
        #search-field::placeholder {
            color: #888;
        }
        
        /* Notification button */
        .icon-button {
            position: relative;
            background: none;
            border: none;
            font-size: 24px;
            color: #333;
            cursor: pointer;
            margin-right: 20px;
        }
        
        .notification-badge {
            position: absolute;
            top: -5px;
            right: -5px;
            background-color: red;
            color: white;
            border-radius: 50%;
            width: 18px;
            height: 18px;
            font-size: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        /* User icon (circle) */
        #user-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: #ddd;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            color: #333;
            cursor: pointer;
            border: 2px solid #ccc;
        }
        
        /* Sidebar styles (touches top edge, full left column) */
        #sidebar {
            position: fixed;
            top: 0; /* Touches top edge */
            left: 0;
            width: 15%;
            height: 100%;
            background-color: #ffffff;
            box-sizing: border-box;
            overflow-y: auto;
            color: white;
            z-index: 999;
        }
        
        /* Optional sidebar header area (e.g., for logo) */
        #sidebar-header {
            height: 60px; /* Matches top bar height */
            background-color: #fcffff; /* Slightly darker for distinction */
            display: flex;
            align-items: center;
            padding: 0 20px;
            color: white;
            font-size: 18px;
            font-weight: bold;
        }

        /* Hide hover icon by default */
        .hover-icon {
            display: none;
            margin-right: 10px; /* Match the default icon's spacing */
            margin-top: 3px;
            font-size: 1.2em;
            width: 20px;
            text-align: center;
        }

        /* On hover over the nav-link, hide default icon and show hover icon */
        #sidebar-content .nav-link:hover > i:first-child {
            display: none; /* Hide the default (first) icon */
        }

        #sidebar-content .nav-link:hover > .hover-icon {
            display: inline-block; /* Show the hover icon */
            color: rgb(0, 0, 0); /* Optional: Keep your existing pink hover color or adjust */
        }
        
        /* Sidebar content (starts below its header) */
        #sidebar-content .nav-link {
            display: flex;
            align-items: center;
            width: 70%;
            padding-left: 20px;
            padding-top: 10px;
            padding-bottom: 10px;
            margin-bottom: 10px;
            margin-top: 10px;
            margin-left: 30px;
            margin-right: 100px; /* Fixed from 100px to prevent clipping */
            background-color: #fcffff;
            font-family: 'Inter', sans-serif;
            font-weight: 700;
            color: #939596;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none; /* Remove underline from links */
            font-size: 16px;
            transition: background-color 0.3s;
        }

        #sidebar-content .nav-link:hover {
            background-color: #efefef;
            color: black;
        }

        

        #sidebar-content .nav-link i {
            margin-right: 10px; /* Space between icon and text */
            margin-top: 3px;
            font-size: 1.2em; /* Adjust icon size if needed */
            width: 20px; /* Fixed width for alignment */
            text-align: center;
        }

        #sidebar-content h2 {
            margin-top: 0;
            color: #000; /* Changed from white (#fff) since sidebar bg is light (#fcffff); adjust if needed */
        }
        
        /* Main content styles */
        #main-content {
            margin-left: 15%;
            margin-top: 60px; /* Space for the (now non-overlapping) top bar */
            padding: 20px;
            min-height: calc(100vh - 60px); /* Ensure it fills the view */
        }
        
        /* iframe styles to fill the main content area */
        #content-frame {
            width: 100%;
            height: 100%;
            min-height: calc(100vh - 80px); /* Adjust for padding, fills the viewport minus header/padding */
            border: none;
            display: block;
        }
    </style>
    <script>
        // Function to load content into the iframe
        function loadContent(page) {
            document.getElementById('content-frame').src = page;
        }

        // Load default content on page load (e.g., discover.html)
        window.onload = function() {
            loadContent('user_discover.html');
        };
    </script>
</head>
<body>
    <!-- Sidebar (now fully touches top, with optional header space) -->
    <nav id="sidebar">
        <div id="sidebar-header">Logo / Title</div> <!-- Customize this area -->
        <div id="sidebar-content">
    <a href="#" class="nav-link" onclick="loadContent('user_discover.html'); return false;">
        <i class="fi fi-rr-home"></i>
        <i class="fi fi-sr-home hover-icon"></i>
        
        <span>Discover</span>
    </a>
    <a href="#" class="nav-link" onclick="loadContent('user_mybooks.html'); return false;">
        <i class="fi fi-rr-book"></i>
        <i class="fi fi-rr-book hover-icon"></i>
        <span>My books</span>
    </a>
    <a href="#" class="nav-link" onclick="loadContent('user_section3.html'); return false;">
        <i class="fi fi-rr-apps"></i>
        <i class="fi fi-rr-apps hover-icon"></i>
        <span>Section 3</span>
    </a>
</div>
    </nav>
    
    <!-- Top bar (now beside sidebar, not overlapping) -->
    <header>
        <input type="text" id="search-field" placeholder="Look for your favourite book">
        <!-- Notification button with badge -->
        <button class="icon-button">
            <i class="fas fa-bell"></i>
            <span class="notification-badge">3</span>
        </button>
        <!-- User icon in circle -->
        <div id="user-icon">
            <i class="fas fa-user"></i>
        </div>
    </header>
    
    <!-- Main content wrapper - Now uses iframe to load external HTML files -->
    <div id="main-content">
        <iframe id="content-frame" src="user_discover.html"></iframe>
    </div>
</body>
</html>


